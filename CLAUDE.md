## Narration

Tool descriptions state the action truthfully. Every read - read, diff, list, search, grep,
status, log - is narrated "Inspecting ...", no synonyms. Never phrase a read as a write:
`git diff | grep '^+'` is "Inspecting added lines in cook SKILL.md", not "Added lines in
cook SKILL.md".
